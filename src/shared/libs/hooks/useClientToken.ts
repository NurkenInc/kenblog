import { useEffect, useState } from 'react';
// import { fetchToken } from '../configs/session/actions';

export function useClientToken() {
  // const [token, setToken] = useState();

  // useEffect(() => {
  //   const getToken = async () => {
  //     const token = await fetchToken();
  //     setToken(token);
  //   }
  //   getToken();
  // }, [])

  // return { token }
}