// import { Post } from '@/entities/Post';
// import { StateSchema } from '@/globals/providers/StoreProvider/config/StateSchema';
// import { createEntityAdapter } from '@reduxjs/toolkit';
// import { createSlice } from '@reduxjs/toolkit';

// const blogsAdapter = createEntityAdapter<Post>({})

// export const getBlogs = blogsAdapter.getSelectors<StateSchema>(
//   (state) => state.blogsPage || blogsAdapter.getInitialState();
// )

// const blogsPageSlice = createSlice({
//   name: 'blog/blogsPageSlice',
//   initialState: blogsAdapter.getInitialState<>
// })

